We'll provide both the default program and test program for each individual languages.

We'll use soukets for the 'expose as a service' functionality.

We'll do a script to write an HTML documentation based on 
the 'tests' files.
